document.addEventListener('DOMContentLoaded', () => {
    // Fetch art data from the Artsy API
    fetchArtData();
  
    function fetchArtData() {
      // Make an API request to retrieve art data from Artsy
      // Handle the response and display the art on the page
      // You can use the fetch() API for this purpose
    }
  });
  